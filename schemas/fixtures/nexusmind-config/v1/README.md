# NexusMind config v1 conformance fixtures

`cases.json` is the cross-language contract. Every consumer must agree on fixture validity, stable
error conditions, and normalized routing results. Changing an existing expectation requires deciding
whether the schema version must change; consumers must never reinterpret a published fixture silently.
