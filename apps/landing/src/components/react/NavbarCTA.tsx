import { Button } from '../ui/Button';

export default function NavbarCTA() {
  return (
    <a href="#waitlist">
      <Button variant="primary" size="sm">
        Join Waitlist
      </Button>
    </a>
  );
}
