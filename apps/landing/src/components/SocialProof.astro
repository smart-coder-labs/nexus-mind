---
// Social Proof section
---

<section class="py-14 px-4" data-reveal>
  <div class="max-w-6xl mx-auto text-center">
    <p class="text-xs font-semibold tracking-widest text-slate-500 uppercase mb-8">Trusted by forward-thinking teams</p>
    <div class="flex flex-wrap justify-center gap-x-12 gap-y-5 text-slate-600 text-lg font-semibold opacity-50">
      {['Acme Corp', 'Quantum Labs', 'DataFlow Inc', 'Nebula Systems', 'Polaris AI'].map(name => (
        <span class="hover:opacity-80 transition-opacity duration-300">{name}</span>
      ))}
    </div>
  </div>
</section>
